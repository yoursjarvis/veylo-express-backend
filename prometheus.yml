global:
  scrape_interval: 5s

rule_files:
  - "prometheus_alerts.yml"
  - "slo_alerts.yml"

scrape_configs:
  - job_name: "otel-collector"
    static_configs:
      - targets: ["otel-collector:8889"]

  - job_name: "backend"
    static_configs:
      - targets: ["backend:4000"]
